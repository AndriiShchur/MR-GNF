# Core ML stack
# NOTE: Install PyTorch separately with the correct CUDA/CPU wheel, e.g.:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pytorch-lightning>=2.2.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
tqdm>=4.65.0
typing_extensions>=4.8.0
lightning-utilities>=0.10.0
rich>=13.3.0

# Data & arrays
xarray>=2023.6.0
netCDF4>=1.6.0
zarr>=2.14.0
fsspec>=2023.6.0
numcodecs>=0.11.0
pyarrow>=12.0.0

# Geospatial (static features, masks, etc.)
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.5.0

# Plotting / notebooks
matplotlib>=3.7.0
jupyterlab>=4.0.0
ipykernel>=6.23.0

# Optional (uncomment if needed)
# cartopy>=0.22.0           # map projections (requires GEOS/PROJ system libs)
# jigsawpy>=0.3.4           # unstructured mesh generation (if you use JIGSAW)
